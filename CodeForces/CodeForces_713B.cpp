#include <bits/stdc++.h>

using namespace std;

int main()
{
    // Size of the grid (n*n)
    int n;
    cin >> n;
    // Vamos considerar pesquisa binaria vertical e horizontal
    int low1 = 1, low2 = 1;
    int high1 = n, high2 = n;
    // Horizontal
    while (low1 <= high1)
    {
        int middle = low + (high - low) / 2;
        cout << "?"
        // Fazer algo
    }
    return 0;
}